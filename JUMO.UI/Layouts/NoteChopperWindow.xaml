<Window x:Class="JUMO.UI.Layouts.NoteChopperWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:JUMO.UI.Layouts"
        xmlns:ctrl="clr-namespace:JUMO.UI.Controls"
        mc:Ignorable="d"
        Title="자르기" Height="200" Width="300">
    <Window.Resources>
        <ResourceDictionary Source="../Resources/Skin.xaml" />
    </Window.Resources>
    <DockPanel>
        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="확인" Click="CheckButtonClick"/>
            <Button Content="취소" Command="{Binding AbortCommand}" Click="ExitButtonClick"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
            <StackPanel Width="50" HorizontalAlignment="Center">
                <TextBlock Text="간격" TextAlignment="Center"/>
                <ctrl:Knob Minimum="0" Maximum="16" TrackPadding="5" TrackLength="2" KnobRadius="13" Value="{Binding ChopInterval}"/>
            </StackPanel>
        </StackPanel>
    </DockPanel>
</Window>
