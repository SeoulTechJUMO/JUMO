<UserControl x:Class="JUMO.UI.Views.MixerVstView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:JUMO.UI.Views"
             mc:Ignorable="d">

    <Border BorderBrush="Gray" BorderThickness="1">
        <StackPanel Width="200" Height="20" Orientation="Horizontal" Margin="2">
            <StackPanel.InputBindings>
                <MouseBinding Gesture="LeftClick" Command="{Binding DataContext.OpenPluginEditorCommand, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}}" CommandParameter="{Binding}"/>
            </StackPanel.InputBindings>
            <TextBlock Text="{Binding Name}" Width="180"/>
            <Button Padding="4,2" Content="+" HorizontalAlignment="Center" Command="{Binding DataContext.AddPluginCommand, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}}"/>
        </StackPanel>
    </Border>
</UserControl>
